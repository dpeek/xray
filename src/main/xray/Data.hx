package xray;

import haxe.macro.Expr;

typedef TypeInfo = {
	var name:String;
	var pos:Position;
	// fields:Array<FieldInfo>
}

// typedef FieldInfo = {
// 	name:String,
// 	min:Int,
// 	max:Int
// }
